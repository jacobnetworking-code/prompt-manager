# Prompt Manager M1.7.11.3 — Platform Model Registry

Model is now populated from a curated platform registry, not from the user's saved prompts.
Unknown/custom models already present in Library metadata are appended rather than discarded.

Replace: index.html, model-support.js, sw.js
Add: model-registry.js
Delete: none
SQL: none

Commit: M1.7.11.3 — Add Platform Model Registry
