/* Prompt Manager V1.8.1 hotfix — translation layer disabled safely.
   Keep the V1.8 runtime as the source of truth until the static translation
   dataset is complete and validated. This file intentionally performs no
   rendering overrides, event interception, or catalog mutation. */
(()=>{"use strict";
  window.PM_CATALOG_I18N_STATUS=Object.freeze({
    enabled:false,
    reason:"translations-pending"
  });
})();
