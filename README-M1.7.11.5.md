# Prompt Manager M1.7.11.5

Polishes the Library Platform / Model filter row:
- Gives Platform slightly more width so its default label fits.
- Keeps Search as the dominant control.
- Uses the same chevron size and spacing for Platform and Model.
- Selected Platform replaces only the Platform button label.
- Selected Model replaces only the Model button label.
- Keeps all controls on one row on mobile and desktop.

Replace:
- index.html
- model-support.js
- product-ui.css
- sw.js

Add/Delete/SQL: none

Commit:
M1.7.11.5 — Polish Library Filter Controls
